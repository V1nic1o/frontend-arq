# Copiar a Vercel → Settings → Environment Variables (Production)
VITE_API_URL=https://backend-arq-904288589125.us-central1.run.app/api

# Desarrollo local: copiar a frontend/.env.local (misma variable)
# Si prefieres backend local: deja VITE_API_URL sin definir y corre npm run backend:dev
